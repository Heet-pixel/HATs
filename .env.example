PORT=3000
NODE_ENV=development
WHATSAPP_NUMBER=919999999999
CONTACT_EMAIL=hello@hat.dev

# Admin login for /admin (change these!)
ADMIN_EMAIL=heetshah@gmail.com
ADMIN_PASSWORD=12312312
SESSION_SECRET=change-this-to-a-long-random-string

# Lead notifications by email (all contact-form + chatbot leads)
MAIL_TO=heet@email.com
MAIL_FROM=HAT Website <your-smtp-user@example.com>
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-app-password
# Behind a proxy (nginx/Cloudflare)? set to 1 so the admin lockout uses real visitor IPs
# TRUST_PROXY=1
